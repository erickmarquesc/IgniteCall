generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

//Um model é uma tabela no banco
model User {
  id         String   @id @default(uuid())
  username   String   @unique
  name       String
  created_at DateTime @default(now())

  @@map("users")
}

